project('meson-test', 'cpp',
    version : '0.1',
    default_options : [
        'cpp_std=c++14',
        'buildtype=debug',
        'default_library=shared',
        'b_vscrt=md'
    ]
)

# Add subdirectories
subdir('libs/os_utils')
subdir('apps/hello')